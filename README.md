# Enver Landing (Vite + React + TypeScript + Tailwind)

Quick scaffold replicating the provided landing design.

Install dependencies and run:

```bash
npm install
npm run dev
```

Place your image assets in `src/assets` (see `src/assets/README.md`).
